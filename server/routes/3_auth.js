module.exports = (app, mw) => {


  // app.routers['auth'].get('/god/:login', (req, res, next) => {
  //   if (!/dev/.test(config.env) && !/^155.143.23/.test(req.ctx.ip))
  //     return res.status(404).send('')

  //   honey.model.DAL.User.getByQuery({'auth.gh.login':req.params.login}, (e, existing) => {
  //     if (e || !existing) return res.status(404).send('login not found')
  //     var tokens = existing.auth.gh.tokens.athr
      // app.meanair.logic.auth.loginAuthor.exec.call(req, 'github', existing.auth.gh, tokens, (e,r) => {
      //   if (e) return next(e)
      //   req.locals.r = r
      //   req.logIn(r, next)
      // })
  //   })
  // })


}


