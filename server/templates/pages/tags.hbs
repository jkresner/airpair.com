{{> siteNav }}

<section id="tagsall">

    <h1>Learn to code by programing skills</h1>
    <ul class="tags">
{{#each r.tags }}<li><a href="{{url}}">{{name}}</a> ({{posts}})</li>{{/each}}
    </ul>

</section>
