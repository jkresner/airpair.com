chat-header
  chat-notifications
    img(src="/v1/img/css/chat/toggle.png")
  channel-name
    | {} Prateek Gupta
other-person
  img(ng-src="other.picture")
  br
  .title {{ other.title }}
messages
  ul
    li(ng-repeat="msg in messages") {{ msg.content }}
entry
  form(ng-submit='say(message)')
    input#message(ng-model='message', ng-focus='clearNotifications(activeChannel, user)', placeholder='ask a question')
